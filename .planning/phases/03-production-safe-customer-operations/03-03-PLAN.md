---
phase: 03-production-safe-customer-operations
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - worker/src/db/seed.ts
  - worker/src/migrate/import-local-store.ts
  - worker/src/routes/migrate.ts
  - worker/src/security.ts
  - worker/src/validation.ts
  - worker/test/api.integration.test.ts
  - worker/test/security.test.ts
  - src/data/seed-data.ts
  - e2e/console.spec.ts
  - .planning/phases/03-production-safe-customer-operations/VERIFICATION.md
autonomous: true
requirements:
  - SAFE-06
must_haves:
  truths:
    - "Fresh deployments seed D1 directly from the canonical deterministic seed with stable IDs and SEED_NOW timestamps; the operator-triggered import path moves the localStorage demo store into D1 exactly once and re-running never duplicates records (D-11)."
    - "The Worker is hardened: typed server-side input validation on every DTO, a consistent { error: { code, message } } envelope, rate limiting on mutation routes, CORS restricted to the console origin, and baseline security headers retained (SAFE-06, ASVS V5/V9)."
    - "The API-backed E2E suite covers permission failures, archive/audit journeys, and the principal operator flows (commercial models, credit, usage, statement, low balance) with a deterministic D1 reset path; no conditional skips, sleeps, or weakened assertions (SAFE-06)."
    - "The ledger remains immutable and derived in D1; balances and statements are always derived server-side from append-only rows; no stored balance counter exists anywhere (D-12)."
    - "The console UI remains preserved through the phase close: same four tabs, list, catalog, confirmations, copy, and responsive behavior (D-08)."
    - "The phase closes with VERIFICATION.md recording the full regression gate, desktop/mobile visual verification, and the requirement-to-evidence map for SAFE-01, SAFE-03, SAFE-04, SAFE-05, and SAFE-06."
  artifacts:
    - path: worker/src/db/seed.ts
      provides: "Deterministic D1 seed derived from the canonical seed-data shapes"
    - path: worker/src/migrate/import-local-store.ts
      provides: "Idempotent localStorage-to-D1 import with stable IDs and an import marker guard"
    - path: worker/src/routes/migrate.ts
      provides: "Authenticated operator-triggered import endpoint"
    - path: worker/src/security.ts
      provides: "Rate limiting, CORS restriction, and baseline security headers"
    - path: worker/src/validation.ts
      provides: "Typed server-side DTO validation shared by every route"
    - path: worker/test/security.test.ts
      provides: "Rate-limit, CORS, header, and validation coverage"
    - path: e2e/console.spec.ts
      provides: "Complete API-backed strict no-skip suite including permission failures and principal operator flows"
    - path: .planning/phases/03-production-safe-customer-operations/VERIFICATION.md
      provides: "Phase 3 verification evidence and requirement-to-test map"
  key_links:
    - from: worker/src/routes/migrate.ts
      to: worker/src/migrate/import-local-store.ts
      via: "the authenticated import endpoint runs the idempotent import and returns the import marker state"
      pattern: "importLocalStore|import marker|POST /api/migrate/import"
    - from: worker/src/db/seed.ts
      to: src/data/seed-data.ts
      via: "the D1 seed derives from the canonical deterministic seed shapes with stable IDs and SEED_NOW"
      pattern: "buildSeedStore|SEED_NOW|AGENT_PRODUCTS"
    - from: worker/src/index.ts
      to: worker/src/security.ts
      via: "every /api/* response applies rate limiting, CORS, and baseline security headers"
      pattern: "rateLimit|CORS|security headers"
    - from: worker/src/routes/*.ts
      to: worker/src/validation.ts
      via: "every route validates its typed DTO before any database work"
      pattern: "validateDto|parseRequest|400"
    - from: e2e/console.spec.ts
      to: worker/src/routes/*.ts
      via: "the API-backed suite exercises permission failures and principal operator flows against the local Worker + D1"
      pattern: "401|archive|audit trail|credit|usage|statement"
  prohibitions:
    - "No generic CRUD API, no Refine data provider against the API, and no endpoint that does not map to a console workflow (D-02)."
    - "No DELETE /api/customers/:id; archive retains every dependent record (D-05)."
    - "No Stripe, payment processing, invoicing, tax, or money movement (D-09)."
    - "No stored balance counter in D1; balances are always derived from append-only ledger rows (D-12)."
    - "No session cookies or server-side session store; identity remains per-request from the verified JWT (D-04)."
    - "No redesign of the console UI; no new top-level routes; no fifth profile tab (D-08)."
    - "No conditional E2E skips, sleeps, or weakened assertions; no secrets committed to tracked files."
---

# Plan 03-03 — Production Hardening, Migration and Verification

<objective>
Harden the production path and close Phase 3: a deterministic D1 seed and idempotent localStorage import, server-side hardening (typed validation, consistent error envelope, rate limiting, CORS, security headers), the complete API-backed E2E suite covering permission failures and principal operator flows, and the phase verification gate with desktop/mobile visual verification.

Purpose: Make the durable authorized console safe to operate with real records and prove every Phase 3 requirement through automated and visual verification.
Output: D1 seed/import, hardened Worker middleware and validation, the complete API-backed E2E suite, and `VERIFICATION.md` closing the phase.

<acceptance_criteria>
- SAFE-06 is fully satisfied: automated tests verify commercial-model validation, ledger arithmetic, idempotent usage, permission failures, and principal operator flows through the API-backed SPA.
- Fresh D1 deployments seed deterministically; the operator-triggered import is idempotent and never duplicates records.
- The Worker applies typed input validation, a consistent error envelope, rate limiting, CORS restriction, and baseline security headers.
- The full regression gate (typecheck, lint, unit tests, build, full E2E, git diff --check) passes.
- Desktop (1920×1080 and 1280×800) and mobile (390×844) visual verification records no horizontal overflow or console errors.
- VERIFICATION.md records the requirement-to-evidence map for SAFE-01, SAFE-03, SAFE-04, SAFE-05, and SAFE-06 before this plan may hand off.
</acceptance_criteria>
</objective>

<execution_context>
@C:/Users/snatanzi/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/snatanzi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-production-safe-customer-operations/03-CONTEXT.md
@.planning/phases/03-production-safe-customer-operations/03-RESEARCH.md
@.planning/phases/03-production-safe-customer-operations/03-PATTERNS.md
@.planning/phases/03-production-safe-customer-operations/03-UI-SPEC.md
@.planning/phases/03-production-safe-customer-operations/03-02-PLAN.md
@.planning/phases/03-production-safe-customer-operations/03-02-SUMMARY.md
@.planning/phases/02-prepaid-credit-and-usage-accounting/VERIFICATION.md
@src/data/seed-data.ts
@src/data/local-storage-repository.ts
@worker/src/index.ts
@worker/src/routes/*.ts
@worker/src/errors.ts
@e2e/console.spec.ts
@playwright.config.ts
@wrangler.jsonc
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Ship the deterministic D1 seed, idempotent import, and server hardening</name>
  <reversibility rating="costly">The D1 seed/import contract and the hardened middleware become the canonical production path, but the deterministic IDs and import marker keep re-runs safe and migratable.</reversibility>
  <read_first>src/data/seed-data.ts; src/data/local-storage-repository.ts; worker/src/index.ts; worker/src/errors.ts; .planning/phases/03-production-safe-customer-operations/03-CONTEXT.md decisions D-01, D-07, D-11, and D-12; .planning/phases/03-production-safe-customer-operations/03-PATTERNS.md sections for schema.sql, statements.ts, errors.ts, and seed-data.ts; .planning/phases/03-production-safe-customer-operations/03-RESEARCH.md Patterns 7 and Pitfall 6</read_first>
  <files>worker/src/db/seed.ts, worker/src/migrate/import-local-store.ts, worker/src/routes/migrate.ts, worker/src/security.ts, worker/src/validation.ts, worker/test/api.integration.test.ts, worker/test/security.test.ts, src/data/seed-data.ts</files>
  <action>Per D-01/D-07/D-11/D-12, create worker/src/db/seed.ts deriving the deterministic D1 seed from the canonical seed-data shapes (buildSeedStore, SEED_NOW, AGENT_PRODUCTS) with stable IDs so fresh deployments start from the same six-customer baseline. Create worker/src/migrate/import-local-store.ts implementing the idempotent localStorage-to-D1 import: map the localStorage payload (or the canonical seed) to D1 rows with the same deterministic IDs, guard with an import marker so re-running never duplicates records, and preserve every customer, arrangement, grant, activity, ledger, and usage record. Create worker/src/routes/migrate.ts exposing the authenticated operator-triggered POST /api/migrate/import endpoint that runs the import and returns the marker state. Create worker/src/validation.ts with typed server-side DTO validation (whole-token ranges, required trimmed references/reasons, valid IDs and ISO timestamps, lifecycle enums) shared by every route, and worker/src/security.ts with rate limiting on mutation routes, CORS restricted to the console origin, and baseline security headers applied to every /api/* response. Wire both into worker/src/index.ts so validation runs before any database work and security headers/rate limiting apply to every response. Extend worker/test/api.integration.test.ts (seed determinism, import idempotency on re-run, import preserves every record, validation rejections return 400 with the stable envelope) and add worker/test/security.test.ts (rate-limit enforcement, CORS origin restriction, baseline headers present, malformed DTO rejection).</action>
  <acceptance_criteria>
    - Fresh D1 deployments seed deterministically from the canonical seed with stable IDs and SEED_NOW timestamps.
    - The operator-triggered import is idempotent: re-running never duplicates records and preserves every collection.
    - Every route validates its typed DTO before any database work and returns the stable error envelope on rejection.
    - Rate limiting, CORS restriction, and baseline security headers apply to every /api/* response.
    - Worker integration and security tests prove every invariant above.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- worker/test/api.integration.test.ts worker/test/security.test.ts</automated>
  </verify>
  <done>The deterministic D1 seed, idempotent import, typed validation, rate limiting, CORS, and security headers are implemented and proven by Worker integration and security tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the API-backed E2E suite with permission failures and principal operator flows</name>
  <read_first>e2e/console.spec.ts; playwright.config.ts; .planning/phases/03-production-safe-customer-operations/03-RESEARCH.md Verification Strategy and Requirement-to-Test Map; .planning/phases/03-production-safe-customer-operations/03-PATTERNS.md e2e section</read_first>
  <files>e2e/console.spec.ts</files>
  <action>Keep the deterministic D1 reset path and role/label/testid queries; do not copy conditional skip behavior. Add strict no-skip scenarios covering the full Phase 3 surface: (1) "permission failures" — an unauthenticated request to a mutation endpoint returns 401 and writes nothing, and the SPA renders the sign-in-required state; (2) "archive and audit journey" — archive a customer, assert it leaves the default list, appears under the Archived filter, its profile shows the banner with retained records readable, and the Operator audit trail shows the archival with the operator identity; (3) "principal operator flows" — create a customer, assign each commercial model, grant and revoke agent access, add credit, record usage with exact-replay no-op and conflict rejection, apply an adjustment and a reversal, filter the statement, and verify the low-balance badge — all against the API-backed SPA with reload-persistence assertions; (4) "import idempotency" — trigger the authenticated import twice and assert no duplicate records. Assert exact high-impact copy and persisted results; fail loudly when any required control or state is absent. After the E2E scenarios pass, run the full regression gate (typecheck, lint, unit tests, build, full E2E, git diff --check).</action>
  <acceptance_criteria>
    - Permission failures are proven end to end: unauthenticated mutations are rejected and the SPA fails closed.
    - The archive and audit journey works end to end with retained records and operator identity in the audit trail.
    - Every principal operator flow (commercial models, agent access, credit, usage idempotency, adjustment, reversal, statement, low balance) works against the API-backed SPA and survives reload.
    - The authenticated import is idempotent when triggered twice.
    - No conditional skips, sleeps, or weakened assertions are introduced; the pre-existing console regression flows remain green.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- e2e/console.spec.ts --grep "permission|archive|audit|principal|import"</automated>
  </verify>
  <done>The API-backed E2E suite covers permission failures, archive/audit journeys, principal operator flows, and import idempotency, and the full regression gate is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close the phase with visual verification and VERIFICATION.md</name>
  <read_first>.planning/phases/02-prepaid-credit-and-usage-accounting/VERIFICATION.md; .planning/phases/03-production-safe-customer-operations/03-UI-SPEC.md Objective Acceptance Criteria and Responsive Contract; .planning/phases/03-production-safe-customer-operations/03-RESEARCH.md Verification Strategy</read_first>
  <files>.planning/phases/03-production-safe-customer-operations/VERIFICATION.md</files>
  <action>Run the complete final gate: npm run typecheck, npm run lint, npm test, npm run build, npm run test:e2e, and git diff --check. Perform desktop (1920×1080 and 1280×800) and mobile (390×844) visual verification of the customer list, profile (Overview, Commercial, Agent Access, Activity), archived profile, audit trail, identity line, and sign-in-required state: no page-level horizontal overflow, no console errors, tables use bounded scrolling, sheets full-width on mobile, and confirmations operable. Record screenshots under .artifacts/phase-03/. Write .planning/phases/03-production-safe-customer-operations/VERIFICATION.md with the requirement-to-evidence map for SAFE-01, SAFE-03, SAFE-04, SAFE-05, and SAFE-06, the commands and results, the visual-verification evidence, known residual risks, and the exact commit(s).</action>
  <acceptance_criteria>
    - The full regression gate passes with no failures.
    - Desktop and mobile visual verification records no horizontal overflow or console errors across the list, profile, archived profile, audit trail, identity, and sign-in-required states.
    - VERIFICATION.md records the requirement-to-evidence map, commands and results, visual evidence, residual risks, and the exact commit(s).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test && npm run build && npm run test:e2e && git diff --check</automated>
  </verify>
  <done>The phase closes with a green full gate, desktop/mobile visual verification evidence, and VERIFICATION.md recording every Phase 3 requirement as verified.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `worker/src/db/seed.ts`: deterministic D1 seed derived from the canonical seed-data shapes.
- `worker/src/migrate/import-local-store.ts` and `worker/src/routes/migrate.ts`: idempotent authenticated localStorage-to-D1 import.
- `worker/src/security.ts` and `worker/src/validation.ts`: rate limiting, CORS, security headers, and typed server-side DTO validation.
- `worker/test/api.integration.test.ts` and `worker/test/security.test.ts`: seed/import idempotency, validation, rate-limit, CORS, and header coverage.
- `e2e/console.spec.ts`: complete API-backed strict no-skip suite including permission failures and principal operator flows.
- `.artifacts/phase-03/`: desktop and mobile visual verification screenshots.
- `.planning/phases/03-production-safe-customer-operations/VERIFICATION.md`: phase verification evidence and requirement-to-test map.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-triggered import to D1 | Untrusted localStorage payloads cross into the durable database. |
| client requests to hardened Worker | Untrusted DTOs, origins, and request rates cross into the API. |
| D1 rows to E2E assertions | The deterministic seed and reset path define the expected state. |

## STRIDE Threat Register (ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Tampering | import path | high | mitigate | Import is authenticated, deterministic, and guarded by an import marker so re-running never duplicates records (D-11). |
| T-03-14 | Tampering | DTO validation | high | mitigate | Typed server-side validation runs before any database work; malformed input returns 400 with the stable envelope (ASVS V5). |
| T-03-15 | Denial of service | mutation routes | medium | mitigate | Rate limiting on mutation routes bounds abuse (ASVS V9). |
| T-03-16 | Information disclosure | CORS/headers | medium | mitigate | CORS restricted to the console origin; baseline security headers retained on every response (ASVS V9). |
| T-03-17 | Repudiation | audit completeness | high | mitigate | The E2E suite asserts audit entries for commercial changes, credit operations, and archival with operator identity (SAFE-05, SAFE-06). |
| T-03-18 | Tampering | ledger arithmetic | high | mitigate | The E2E suite re-verifies derived balances, idempotent usage, and reversal netting against the API-backed SPA (D-12, SAFE-06). |
</threat_model>

<verification>
Targeted: `npm test -- worker/test/api.integration.test.ts worker/test/security.test.ts`; `npm run test:e2e -- e2e/console.spec.ts --grep "permission|archive|audit|principal|import"`.
Full regression: `npm run typecheck && npm run lint && npm test && npm run build && npm run test:e2e && git diff --check`.
Visual verification: desktop 1920×1080 and 1280×800, mobile 390×844 — no page-level horizontal overflow, no console errors, tables bounded scrolling, sheets full-width on mobile, confirmations operable.
</verification>

<stop_go_gate>
GO only when all three tasks are implemented, every targeted check passes, the full regression command passes, `git diff --check` is clean, desktop/mobile visual verification records no overflow or console errors, and `VERIFICATION.md` records files changed, commands and results, the requirement-to-evidence map for SAFE-01/SAFE-03/SAFE-04/SAFE-05/SAFE-06, visual-verification evidence, known residual risks, and the exact commit(s). STOP on any failure: do not close the phase; preserve working tree and test artifacts, report the failing command and first actionable error, and make no unrelated cleanup or rollback.
</stop_go_gate>

<success_criteria>
- SAFE-06 is fully satisfied: automated tests verify commercial-model validation, ledger arithmetic, idempotent usage, permission failures, and principal operator flows through the API-backed SPA.
- Fresh D1 deployments seed deterministically; the operator-triggered import is idempotent.
- The Worker applies typed validation, a consistent error envelope, rate limiting, CORS restriction, and baseline security headers.
- The full regression gate is green and desktop/mobile visual verification records no overflow or console errors.
- VERIFICATION.md records every Phase 3 requirement as verified and the phase handoff is complete.
</success_criteria>

<requirement_mapping>
| Requirement | Delivered by | Evidence |
|-------------|--------------|----------|
| SAFE-06 | Task 1 validation/security tests + Task 2 API-backed E2E (permission failures, archive/audit, principal flows, import idempotency) + Task 3 full gate | Worker, security, and E2E tests |
| SAFE-01 | Task 2 archive journey re-verified end to end | E2E tests |
| SAFE-03 | Task 1 D1 seed/import + Task 2 reload-persistence assertions | Worker integration and E2E tests |
| SAFE-04 | Task 2 permission-failure journeys | E2E tests |
| SAFE-05 | Task 2 audit-trail journey with operator identity | E2E tests |
</requirement_mapping>

<prohibitions>
- No generic CRUD API, no Refine data provider against the API, and no endpoint that does not map to a console workflow (D-02).
- No DELETE /api/customers/:id; archive retains every dependent record (D-05).
- No Stripe, payment processing, invoicing, tax, or money movement (D-09).
- No stored balance counter in D1; balances are always derived from append-only ledger rows (D-12).
- No session cookies or server-side session store; identity remains per-request from the verified JWT (D-04).
- No redesign of the console UI; no new top-level routes; no fifth profile tab (D-08).
- No conditional E2E skips, sleeps, or weakened assertions; no secrets committed to tracked files.
</prohibitions>

<output>
Create `.planning/phases/03-production-safe-customer-operations/VERIFICATION.md` when done. Stop after this plan; Phase 3 is complete only after the stop/go gate is GO and the verification record exists.
</output>