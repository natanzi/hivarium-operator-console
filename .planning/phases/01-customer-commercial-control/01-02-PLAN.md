---
phase: 01-customer-commercial-control
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/domain/types.ts
  - src/domain/commercial-rules.ts
  - src/domain/commercial-rules.test.ts
  - src/data/local-storage-repository.ts
  - src/data/local-storage-repository.test.ts
  - src/data/seed-data.ts
autonomous: true
requirements:
  - COMM-01
  - COMM-03
  - COMM-04
  - COMM-06
  - AGNT-02
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Exactly one commercial arrangement is active as of any instant; monthly, prepaid, and annual shapes are all validated and representable (COMM-01; D-01)."
    - "Prepaid stores and reports a USD monetary balance without introducing ledger transactions or usage arithmetic (COMM-03; D-03)."
    - "Annual contracts retain USD value, start/end dates, renewal status, notes, included allowance, allowance unit, and overage rate without calculating overage (COMM-04; D-03)."
    - "Immediate and scheduled changes preserve replaced arrangements as immutable history, including reason and effective range (COMM-06; D-02)."
    - "Agent revocation can be immediate or scheduled and never deletes its grant record (AGNT-02; D-05)."
    - "Expiry or termination revokes every currently active grant in the same atomic transition, appends linked activity events, and a later arrangement never restores them (D-04, D-09)."
  artifacts:
    - path: src/domain/commercial-rules.ts
      provides: "Complete model transitions, date-boundary projections, termination and auto-revoke plans"
    - path: src/domain/commercial-rules.test.ts
      provides: "Table-driven pure-rule coverage"
    - path: src/data/local-storage-repository.ts
      provides: "Atomic immediate/scheduled change, revoke, expiry reconciliation, and immutable history"
  key_links:
    - from: src/data/local-storage-repository.ts
      to: src/domain/commercial-rules.ts
      via: "all lifecycle mutations consume pure transition results and commit one DataStore"
      pattern: "applyCommercialTransition|applyAccessRevocation|reconcileCommercialLifecycle"
    - from: CommercialArrangement termination event
      to: AgentAccessGrant revocation events
      via: "shared causationId in one activity append"
      pattern: "causationId"
  prohibitions:
    - "No external API, backend, ORM, schema push, payment, ledger, usage debit, annual overage calculation, chart, or new dependency."
    - "No mutable audit/history entries, hard-deletes, timer/sleep-driven transitions, or restoration of old grants."
    - "No duplicated commercial rule in React components or competing active legacy models."
---

<objective>
Complete the commercial and access lifecycle behind the proven Phase 1 repository seam.

Purpose: Make all three commercial models, transitions, history, scheduled revocation, and termination consequences deterministic and auditable before building the full UI.
Output: Exhaustive pure rules, atomic repository mutations, lifecycle fixtures, and regression tests.

<acceptance_criteria>
- COMM-01, COMM-03, COMM-04, COMM-06, and AGNT-02 are enforced by repository and pure-rule tests.
- Date boundaries, invalid inputs, immediate/scheduled replacements, immutable history, termination/expiry cascade, linked Activity, and no-restoration behavior are deterministic.
- The targeted and full regression gates pass before 01-03 may start.
</acceptance_criteria>
</objective>

<execution_context>
@C:/Users/snatanzi/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/snatanzi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-customer-commercial-control/CONTEXT.md
@.planning/phases/01-customer-commercial-control/01-UI-SPEC.md
@.planning/phases/01-customer-commercial-control/01-PATTERNS.md
@.planning/phases/01-customer-commercial-control/01-01-SUMMARY.md
@src/domain/types.ts
@src/domain/commercial-rules.ts
@src/data/local-storage-repository.ts
@src/data/local-storage-repository.test.ts
@src/data/seed-data.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete all commercial shapes and transitions</name>
  <precondition>`.planning/phases/01-customer-commercial-control/01-01-SUMMARY.md` exists and records a GO result for every 01-01 targeted and full-regression command.</precondition>
  <read_first>src/domain/types.ts; src/domain/commercial-rules.ts; src/data/local-storage-repository.ts; src/data/local-storage-repository.test.ts; .planning/phases/01-customer-commercial-control/CONTEXT.md Commercial arrangements; .planning/phases/01-customer-commercial-control/01-UI-SPEC.md Commercial tab and State Contract</read_first>
  <files>src/domain/types.ts, src/domain/commercial-rules.ts, src/domain/commercial-rules.test.ts, src/data/local-storage-repository.ts, src/data/local-storage-repository.test.ts</files>
  <behavior>
    - "At an explicit asOf, zero or one record is active; a future successor is scheduled and does not displace current state early."
    - "Prepaid accepts non-negative integer currentBalanceCents in USD plus optional expiry; annual accepts non-negative contractValueCents, includedAllowance, allowanceUnit, overageRateCentsPerUnit, ordered term, renewalStatus, and notes."
    - "Immediate replacement closes the current effective range and appends the successor; scheduled replacement keeps current active until the boundary; history is newest first and unmodified."
    - "Missing legacy fields remain null/undefined and renderable as Not recorded; invalid negative money, NaN/fractional cents, invalid ISO timestamps, and inverted ranges reject without writes."
  </behavior>
  <action>Per D-01/D-02/D-03, finish the PrepaidCommercialArrangement and AnnualCommercialArrangement discriminants and shared status/effective-range semantics. Use integer cents for all USD values; use an explicit allowanceUnit union and integer includedAllowance; persist terms but perform no usage/overage math. Implement validateCommercialArrangement, applyCommercialTransition(store, input, occurredAt), and projectCommercialState(records, asOf) as pure functions. Preserve immutable prior objects, set replacedByArrangementId/effectiveTo/reason when replacement takes effect, distinguish scheduled from current state at the exact ISO boundary, and generate a typed commercial ActivityEvent. Wire saveCommercialArrangement through the pure transition and one repository write. Add table-driven unit cases and repository contract cases for all shapes, boundaries, repeated reads, and rejection atomicity.</action>
  <verify>
    <automated>npm test -- src/domain/commercial-rules.test.ts src/data/local-storage-repository.test.ts -t "commercial"</automated>
  </verify>
  <done>All three arrangements and immediate/scheduled replacement have one canonical, deterministic, immutable, tested outcome for every specified boundary and invalid-input case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete revocation, termination, expiry, and linked history</name>
  <read_first>src/domain/commercial-rules.ts; src/domain/commercial-rules.test.ts; src/data/local-storage-repository.ts; src/data/local-storage-repository.test.ts; src/data/seed-data.ts; .planning/phases/01-customer-commercial-control/CONTEXT.md Agent access and Data/Behavior Constraints</read_first>
  <files>src/domain/commercial-rules.ts, src/domain/commercial-rules.test.ts, src/data/local-storage-repository.ts, src/data/local-storage-repository.test.ts, src/data/seed-data.ts</files>
  <behavior>
    - "Immediate revocation changes as-of access immediately; scheduled revocation remains active before the boundary and revoked at/after it."
    - "Termination or expiry closes the arrangement, revokes every active grant, and appends a triggering commercial event plus one separate access event per grant sharing its causationId."
    - "Future/scheduled grants are not silently made active by a new arrangement, and previously revoked grants never restore when another arrangement starts."
    - "A repeated reconcile at the same asOf is idempotent and does not duplicate revocation or Activity records."
  </behavior>
  <action>Per D-04/D-05/D-09, add revokeAgentAccess(input, occurredAt), terminateCommercialArrangement(input, occurredAt), and reconcileCommercialLifecycle(customerId, asOf) repository methods backed by pure applyAccessRevocation and lifecycle reconciliation. Require a reason; support immediate or explicit future effective date; retain the original grant and record scheduledRevokeAt/revokedAt rather than deleting. For termination/expiry, compute every affected arrangement, grant, and ActivityEvent before a single write; use one stable causationId to link the commercial trigger to separate automatic-revocation events with source "system". Make idempotency depend on stable event/transition identity, not insertion position. Extend deterministic seed data to cover monthly, prepaid, annual, scheduled successor, scheduled revoke, expired history, and terminated-with-revocations, while retaining exactly six customers and the read-only catalog.</action>
  <verify>
    <automated>npm test -- src/domain/commercial-rules.test.ts src/data/local-storage-repository.test.ts -t "revok|terminat|expir|causation|restore"</automated>
  </verify>
  <done>Immediate/scheduled revocation and arrangement termination/expiry are atomic, idempotent, historically retained, causally linked, and never auto-restore old access.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `src/domain/types.ts`: CommercialArrangementBase, MonthlyCommercialArrangement, PrepaidCommercialArrangement, AnnualCommercialArrangement, CommercialArrangement, CommercialArrangementStatus, AgentAccessGrant, AgentAccessStatus, ActivityEvent, ActivitySource, schemaVersion 2 DataStore, and migration-only legacy shapes.
- `src/domain/commercial-rules.ts`: validateCommercialArrangement, projectCommercialState, applyCommercialTransition, resolveAgentAccessStatus, applyAccessRevocation, reconcileCommercialLifecycle, and stable event-ID helpers.
- `src/data/local-storage-repository.ts`: migrateStore and all HiveRepository commercial/access/activity/reverse-query methods.
- `src/domain/commercial-rules.test.ts`, `src/data/local-storage-repository.test.ts`: pure and adapter contract coverage.
- `src/data/seed-data.ts`: deterministic six-customer canonical dataset spanning every Phase 1 state.
- `src/features/customers/components/CommercialArrangementSheet.tsx`, `AgentAccessSheet.tsx`, `ActivityTimeline.tsx`; customer profile page/test; agent catalog/detail pages/tests; router; and unconditional E2E flows completed by 01-03.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository mutation to immutable history | A requested transition can affect multiple canonical records and events. |
| explicit asOf time to lifecycle projection | Boundary timestamps decide commercial and access authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | applyCommercialTransition | high | mitigate | Enforce one-active invariant, integer cents, model-specific fields, ISO ordering, immutable copies, and no-write failures in pure and repository tests. |
| T-01-07 | Tampering | termination/reconciliation transaction | high | mitigate | Precompute arrangement, all grant revocations, and linked events; persist with one DataStore write; test atomic failure and idempotent replay. |
| T-01-08 | Repudiation | automatic revocation history | medium | mitigate | Separate events contain source=system, subject IDs, occurredAt, resulting state, and shared causationId. |
| T-01-09 | Elevation of privilege | resolveAgentAccessStatus | medium | mitigate | Access is granted only for an existing product/customer, within date bounds, and while a qualifying active arrangement exists; new arrangements cannot restore old grants. |
| T-01-10 | Denial of service | large invalid date/money input | low | mitigate | Reject non-finite/non-integer values and invalid ISO strings before sorting or writing. |
</threat_model>

<verification>
Targeted: `npm test -- src/domain/commercial-rules.test.ts src/data/local-storage-repository.test.ts`.
Full regression: `npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run test:e2e &amp;&amp; git diff --check`.
</verification>

<stop_go_gate>
GO only when both tasks, every targeted test, all previously delivered 01-01 flows, the full regression command, and `git diff --check` pass, and `01-02-SUMMARY.md` records commands/results, exact invariant evidence, files changed, residual risks, and commit(s). STOP on any failure: do not begin 01-03; preserve the working tree and test artifacts, report the failing command and first actionable error, and do not weaken assertions, skip scenarios, sleep, roll back history, or proceed conditionally.
</stop_go_gate>

<success_criteria>
- All three models and every lifecycle transition satisfy the locked rules.
- Immutable history and linked automatic revocations survive reload and replay.
- No deferred ledger/backend/payment behavior is introduced.
- The complete stop/go gate is green and the sequential handoff record exists.
</success_criteria>

<output>
Create `.planning/phases/01-customer-commercial-control/01-02-SUMMARY.md` when done. Stop after this plan; coder-stack must receive 01-03 separately only after the stop/go gate is GO.
</output>
